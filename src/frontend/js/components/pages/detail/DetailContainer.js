import { connect } from 'react-redux'

// import * as actionCreators from '../../../redux/action-creators'
import Detail from './Detail'

const DetailContainer = connect()(Detail)
export default DetailContainer
